import React from 'react';

const FooterComponent = () => {
    return (
        <div>
             <footer className = "footer">
                <span className="text-muted">Cloud Class</span>
            </footer>
        </div>
    );
};

export default FooterComponent;